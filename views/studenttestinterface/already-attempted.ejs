<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Taken</title>
   <style>
    /* General Reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Arial', sans-serif;
    background: linear-gradient(135deg, #f1f2f6, #74b9ff);
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    color: #2d3436;
    overflow: hidden;
}

.wrapper {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    height: 100%;
}

.container {
    text-align: center;
    position: relative;
    background-color: #fff;
    border-radius: 20px;
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.1);
    padding: 40px;
    width: 400px;
    animation: fadeIn 1s ease-out;
}

.title {
    font-size: 2.5rem;
    font-weight: bold;
    color: #0984e3;
    margin-bottom: 20px;
    opacity: 0;
    animation: slideInTitle 1s forwards;
}

.description {
    font-size: 1.2rem;
    color: #636e72;
    margin-bottom: 20px;
    opacity: 0;
    animation: fadeInText 1.5s 0.5s forwards;
}

.animation-container {
    position: absolute;
    bottom: -50px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    justify-content: space-evenly;
    width: 100%;
    height: 100px;
}

.circle-1, .circle-2, .circle-3 {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    background: #0984e3;
    opacity: 0;
    animation: bounceIn 2s infinite ease-in-out;
}

.circle-1 {
    animation-delay: 0s;
}

.circle-2 {
    animation-delay: 0.2s;
}

.circle-3 {
    animation-delay: 0.4s;
}

@keyframes fadeIn {
    0% {
        opacity: 0;
        transform: scale(0.8);
    }
    100% {
        opacity: 1;
        transform: scale(1);
    }
}

@keyframes slideInTitle {
    0% {
        opacity: 0;
        transform: translateY(-20px);
    }
    100% {
        opacity: 1;
        transform: translateY(0);
    }
}

@keyframes fadeInText {
    0% {
        opacity: 0;
    }
    100% {
        opacity: 1;
    }
}

@keyframes bounceIn {
    0% {
        opacity: 0;
        transform: translateY(50px);
    }
    50% {
        opacity: 1;
        transform: translateY(-20px);
    }
    100% {
        opacity: 0;
        transform: translateY(50px);
    }
}

   </style>
</head>
<body>
    <div class="wrapper">
        <div class="container">
            <div class="message">
                <h1 class="title">You've Already Taken the Test!</h1>
                <p class="description">It looks like you've already completed the test. Please check back later for your results.</p>
            </div>
            <div class="animation-container">
                <div class="circle-1"></div>
                <div class="circle-2"></div>
                <div class="circle-3"></div>
            </div>
        </div>
    </div>
    <script src="/scripts.js"></script>
</body>
</html>
