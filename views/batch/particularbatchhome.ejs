<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body{
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        }
        #layer1{
            height: 20vh;
            width: 100vw;
            background-color: bisque;
            width: 100%;
        }
        #layer2{
            height: 80vh;
            width: 100vw;
            background-color: aqua;
        }
        .form {
            display: flex;
            flex-direction: column;
            width: 500px;
            background-color: #ffffff;
            padding: 0px;
            border-radius: 5px;
            margin: auto;
            padding: 20px;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
           }

        p {
            text-align: center;
            color: black;
            font-size: 20px;
            margin: 5px 0;
           }

        .btn {
            margin: 20px 0 10px 0;
            background-color: #151717;
            border: none;
            color: white;
            margin: auto;
            font-size: 15px;
            font-weight: 500;
            border-radius: 10px;
            height: 50px;
            width: 100%;
            cursor: pointer;
        }


@media (max-width:450px){
    .form {
        width: 90%;
           }

}

    </style>
</head>
<body>
    <div id="outer-div">
        <div id="layer1">
            <a href="/update-batch/<%=thisBatch._id%>">createbatch</a>
        </div>
        <div id="layer2">

            <div id="outer-div">
                <% if (thisBatch && thisBatch.tests && thisBatch.tests.length > 0) { %>
                    <% for (let i = 0; i < thisBatch.tests.length; i++) { %>
                    <div class="form">
                            <p><%= thisBatch.title%></p>
                        <div class="card-button">
                            <a href="/student/test/<%= thisBatch.tests[i] %>"><button class="btn">Attempt</button></a>   
                        </div>
                    <% } %>
                </div>
                  <% } else { %>
                    <p>No tests available.</p>
                  <% } %>
            </div>  

        </div>
    </div>
</body>
</html>